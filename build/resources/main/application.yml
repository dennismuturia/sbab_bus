server:
  port: 8082

spring:
  h2:
    console.enabled: true
  jpa:
    hibernate.ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

url: https://api.sl.se/api2/linedata.json?
